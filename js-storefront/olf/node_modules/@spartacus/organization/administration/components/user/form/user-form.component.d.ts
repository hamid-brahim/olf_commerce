import { OnInit } from '@angular/core';
import { FormArray, FormControl, FormGroup } from '@angular/forms';
import { B2BUser, B2BUserRole, Title, UserService } from '@spartacus/core';
import { B2BUnitNode, B2BUserService, OrgUnitService } from '@spartacus/organization/administration/core';
import { Observable } from 'rxjs';
import { ItemService } from '../../shared/item.service';
import * as i0 from "@angular/core";
export declare class UserFormComponent implements OnInit {
    protected itemService: ItemService<B2BUser>;
    protected unitService: OrgUnitService;
    protected userService: UserService;
    protected b2bUserService: B2BUserService;
    form: FormGroup;
    /**
     * Initialize the business unit for the user.
     *
     * If there's a unit provided, we disable the unit form control.
     */
    set unitKey(value: string);
    units$: Observable<B2BUnitNode[]>;
    titles$: Observable<Title[]>;
    availableRoles: B2BUserRole[];
    constructor(itemService: ItemService<B2BUser>, unitService: OrgUnitService, userService: UserService, b2bUserService: B2BUserService);
    ngOnInit(): void;
    updateRoles(event: MouseEvent): void;
    get roles(): FormArray;
    get isAssignedToApprovers(): FormControl;
    static ɵfac: i0.ɵɵFactoryDeclaration<UserFormComponent, never>;
    static ɵcmp: i0.ɵɵComponentDeclaration<UserFormComponent, "cx-org-user-form", never, { "unitKey": "unitKey"; }, {}, never, never>;
}
